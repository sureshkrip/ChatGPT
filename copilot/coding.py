













def get_even_numbers(numbers):
    even_numbers = []
    for number in numbers:
        if number % 2 == 0:
            even_numbers.append(number)
    return even_numbers
# This function takes a list of numbers and returns a list of even numbers in that list.
# The function name is get_even_numbers
# The parameter name is numbers
# The variable name for the even numbers is even_numbers
# The for loop iterates over every number in the list
# The if statement checks if the number is even
# The append method adds the number to the list of even numbers















def get_even_numbers(numbers):
    even_numbers = []
    for number in numbers:
        if number % 2 == 0:
            even_numbers.append(number)
    return even_numbers














































































